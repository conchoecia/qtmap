{
  "name": "@qtqc/seed-core",
  "version": "0.0.0",
  "private": true,
  "description": "C11 minimizer + chain + MAPQ + shard codec. Compiles to native CLI (qtqc-mm2-index) and Emscripten WASM (seed_core.{js,wasm}).",
  "license": "MIT",
  "scripts": {
    "build": "echo 'C build is driven by top-level CMake; use scripts/build-native.sh or scripts/build-wasm.sh from repo root'"
  },
  "files": [
    "include/",
    "src/",
    "vendor/",
    "CMakeLists.txt"
  ]
}
